{
	"scripts": {
		"dev:admin": "cd admin && npm run dev",
		"dev:public": "cd public-app && npm run dev",
		"dev:api": "cd api && npm run dev",
		"dev": "npm run dev:admin & npm run dev:public & npm run dev:api",
		"build:admin": "cd admin && npm run build",
		"build:public": "cd public-app && npm run build",
		"build:api": "cd api && npm run build",
		"build": "npm run build:admin && npm run build:public && npm run build:api"
	},
	"devDependencies": {
		"concurrently": "^6.2.0"
	}
}
